android {
    dataBinding {
        enabled = true
    }
}

dependencies {
    api project (path: ':ui-components')

    // AndroidX libs
    api "androidx.recyclerview:recyclerview:$recycler_version"

    // Kotlin libs
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
}