apply from: "$rootProject.projectDir/core-android-module.gradle"

android {
    buildFeatures {
        dataBinding true
    }
    namespace 'com.vgleadsheets.recyclerview'
}

dependencies {
    api project(':core:android:ui-components')

    // AndroidX libs
    api "androidx.recyclerview:recyclerview:$recycler_version"

}
