apply from: "$rootProject.projectDir/core-android-module.gradle"
apply from: "$rootProject.projectDir/compose-module.gradle"

android {
    namespace 'com.vgleadsheets.themes'
}

dependencies {
    // Theming
    implementation project(':core:android:ui:colors')
    api project(':core:android:ui:fonts')
}
