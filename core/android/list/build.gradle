apply from: "$rootProject.projectDir/core-android-module.gradle"
apply from: "$rootProject.projectDir/di-android.gradle"
apply plugin: 'kotlin-kapt'

dependencies {
    api project(':core:android:coroutines')
    api project(':core:android:ui-core')

    implementation project(':core:android:recyclerview')

    implementation project(':features:main:hud')

}

android {
    buildFeatures {
        dataBinding true
    }

    kotlinOptions {
        freeCompilerArgs += "-opt-in=kotlin.RequiresOptIn"
    }
}

